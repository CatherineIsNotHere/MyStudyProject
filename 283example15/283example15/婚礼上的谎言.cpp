#include "stdio.h";

/*
	问题：三对情侣参加婚礼，3个新郎a、b、c，3个新娘x、y、z，有人想知道究竟谁和谁结婚，于是就问新人中的三位，得到如下提示：
	a说他将和x结婚；x说他的未婚夫是c；c说他将和z结婚。这人事后知道他们都在开玩笑，说的全是假话，那么究竟谁与谁结婚呢？
	*/
void main(){
	int a, b, c;
	for (a = 1; a <= 3; a++)
		for (b = 1; b <= 3; b++)
			for (c = 1; c <= 3; c++)
				if (a != 1 && c != 1 && c != 3 && a != b&&a != c&&b != c){
					printf("%c 将嫁给 A\n", 'x' + a - 1);
					printf("%c 将嫁给 B\n", 'x' + b - 1);
					printf("%c 将嫁给 C\n", 'x' + c - 1);
				}
}